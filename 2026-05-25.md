多传感器融合slam
松耦合，通过将激光雷达和IMU分别估计得出参数，后进行参数融合
紧耦合，鲁棒性和准确性更好

slam，基于系统时间戳同步出每帧的数据，再使用卡尔曼滤波进行每帧正确位姿的预测

```
wget http://fishros.com/install -O fishros && . fishros
```

ros2一键安装


Visual（视觉）
Odometry（里程计）
Optimization（优化）
Mapping （建图，绘图）
Landmark （路标，地标）



$$
x_k=f(x_{k-1},v_k,w_k)
$$

位姿（pose）
包含了三维空间的六个自由度（R6）XYZ轴的旋转、平移