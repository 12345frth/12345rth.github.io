小工具
[Personal account](https://grizzlysms.com/profile)

虚拟手机号




publisher（发布者）
subscriber（订阅者）
topic (话题)
actions（行动）
introspection （反省；内省）



topic（话题）





service（服务）
service主要基于请求-相应模型（request-response）
只有在客户端节点发出请求后，服务端才会返回msg

ros2 service list 返回服务列表
ros2 service type <service_name> 查看服务类型
ros2 service list -t  查看所有活跃（active）服务
ros2 service find <type_name>  根据类型查询服务
ros2 interface show <type_name>
ros2 service call <service_name> <service_type> <arguments>  请求服务




